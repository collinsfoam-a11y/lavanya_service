interface:
  display_name: "Lavanya Service"
  short_description: "Repo-specific patterns and workflows for lavanya_service"
  default_prompt: "Use the lavanya_service repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true